// From browser
var MidiWriter = require('.');


var tracks = [];
tracks[0] = new MidiWriter.Track();
tracks[0].addEvent(new MidiWriter.NoteEvent({pitch: ['B4'], duration: '64', repeat: '32'}));

var write = new MidiWriter.Writer(tracks);
write.stdout();
